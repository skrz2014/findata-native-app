definition_version: "1.1"

native_app:
  name: FINDATA_APP
  source_stage: stage_content.app_code
  artifacts:
    - manifest.yml
    - README.md
    - scripts/
    - streamlit/

  package:
    name: FINDATA_APP_PKG
    role: ACCOUNTADMIN
    distribution: internal

  application:
    name: FINDATA_APP
    role: ACCOUNTADMIN
    warehouse: COMPUTE_WH
